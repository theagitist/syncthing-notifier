# Copy this file to config.env and edit. config.env is gitignored.
# BUNDLE_ID is the macOS bundle identifier used by the notification app
# and the launchd agent. Pick something unique to you (reverse-DNS form
# is conventional but not required for local-only use).
BUNDLE_ID=local.syncthing-notifier
